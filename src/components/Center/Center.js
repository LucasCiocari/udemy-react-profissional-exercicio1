import React from 'react';

import "./center.scss";

const Center = ({children}) => {
    return (
        <div className="center">
            {children}
        </div>
    )
}

export default Center;
